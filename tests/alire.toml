name = "tests"
description = ""
version = "0.0.0-test"

executables = ["runner"]

[build-switches]
"*".ada_version = "Ada2022"

[[depends-on]]
bigints = "*"
rand = "0.1.0"

[[pins]]
bigints = { path = '..' }

[build-profiles]
bigints = 'validation'
tests = 'validation'

