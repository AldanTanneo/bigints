name = "tests"
description = ""
version = "0.0.0-test"

executables = ["runner"]

[build-switches]
"*".ada_version = "Ada2022"

[[depends-on]]
bigints = "*"

[[pins]]
bigints = { path = '..' }

[build-profiles]
bigints = 'validation'
tests = 'validation'

